# Asset Management System

电子设备资产管理系统 — 面向单人使用的本地浏览器应用，管理公司电子设备从采购、分配、升级到报废的全生命周期。

## Language

### Core Entities

**Asset（设备/资产）**：
物理存在的电子设备实体，有唯一编号。一旦生成即独立存在，拥有完整的生命周期履历。
_Avoid_: 机器、设备实体

**Asset No（设备编号）**：
设备的唯一标识符，格式为「分类前缀 + 自增序号」（如 DN-0001）。
_Avoid_: 资产编号、序列号

**Asset Category（设备分类）**：
设备的大类划分，两级结构（如「计算机设备 → 笔记本电脑」）。用于编号前缀和统计分组。
_Avoid_: 设备类型

**Device Template（设备模板/BOM）**：
标准配置的定义，描述一类设备由哪些配件组成。用于快速生成设备实体。
_Avoid_: 设备型号、配置模板、BOM

**Component（配件）**：
组成设备的零部件，如 CPU、内存、硬盘等。有独立的库存管理。
_Avoid_: 零部件、物料

**Component Category（配件分类）**：
配件的两级分类结构（如「存储设备 → 固态硬盘」）。
_Avoid_: 配件类型

**Component Model（配件型号）**：
具体的配件产品型号（如「i7-12700F」「金士顿 DDR4 16GB」），有对应库存。
_Avoid_: 配件SKU

**Component Stock（配件库存）**：
某一配件型号的可用数量。采购入库增加，组装设备/升级替换减少。
_Avoid_: 配件余量

### Lifecycle

**Allocation（分配）**：
将设备分配给员工使用，设备状态从「闲置」变为「在用」。支持按模板生成新设备分配，或从闲置设备池中选择分配。支持批量分配多种设备。
_Avoid_: 领用、发放

**Return（归还）**：
员工将设备退回，设备状态从「在用」变回「闲置」，设备实体保留。支持批量归还。
_Avoid_: 退回、入库

**Transfer（调拨）**：
设备直接从一个员工转给另一个员工，不走归还再分配流程。支持批量调拨。
_Avoid_: 划转、移交

**Upgrade（升级）**：
更换设备的配件配置。升级时扣减新配件库存，旧配件回库。设备仍归属于原模板分类，但实际配置以当前为准。
_Avoid_: 配置变更、维修替换

**Maintenance（维修）**：
设备的维修记录，关联维修类型、内容、日期。
_Avoid_: 维保

**Scrap（报废）**：
设备生命周期结束，状态变为「报废」。设备记录保留但不再参与分配，配件不回库。
_Avoid_: 销毁、退役

### Status

**Idle（闲置）**：
设备在库中未分配给任何人，可被分配。
_Avoid_: 在库、空闲

**In Use（在用）**：
设备已分配给员工使用。
_Avoid_: 使用中、已分配

**In Maintenance（维修中）**：
设备处于维修状态，暂不可分配。
_Avoid_: 维修、故障

**Scrapped（报废）**：
设备已报废，不再参与任何流转。
_Avoid_: 已报废、废弃

### Employees & Organization

**Employee（员工）**：
使用设备的人员，有工号、姓名、所属部门、联系方式。
_Avoid_: 用户、人员

**Department（部门）**：
公司内部的组织单元，员工归属于部门。
_Avoid_: 组织、处室

### Inventory & Stocktake

**Asset Pool（设备池）**：
所有设备实体的集合，包含全部状态的设备。按状态筛选可得到闲置设备列表。
_Avoid_: 设备库、资产池

**Stocktake（盘点）**：
核对系统记录与实际物理设备一致性的操作，手动标记每台设备状态，生成盘盈盘亏结果。
_Avoid_: 对账、库存盘点

**Stocktake Session（盘点任务）**：
一次盘点活动，包含盘点范围、开始时间、完成状态和明细记录。
_Avoid_: 盘点单

### Reporting & Logging

**System Log（系统日志）**：
重要操作的审计记录，包括分配、归还、调拨、报废、升级等关键动作。
_Avoid_: 操作日志、审计日志

**Stats（统计报表）**：
多维度的数据统计展示，包括按状态/分类/部门/员工/配件的统计，以及流转趋势。
_Avoid_: 报表、数据分析

### System

**Admin（管理员）**：
系统的唯一操作用户，单账号简单登录。
_Avoid_: 用户、操作员

**Label（标签）**：
可打印的设备信息贴纸，包含设备编号、名称、配置、部门、使用人等信息。纯文字，不含二维码。
_Avoid_: 资产标签、二维码

## Business Contexts

**资产管理上下文（Asset Management）**：
设备的分类、模板、实体与生命周期。核心实体：Asset、AssetCategory、DeviceTemplate、AssetComponent、LifecycleLog。职责：编号生成、状态流转、履历记录。
_Avoid_: 把配件库存、组织人员塞进本上下文

**配件库存上下文（Inventory）**：
配件的分类、型号与库存。核心实体：ComponentCategory、ComponentModel、ComponentStock、ComponentStockLog。职责：采购入库、组装出库、升级扣减与回库。
_Avoid_: 设备本身的归属与状态

**组织上下文（People & Organization）**：
员工与部门。核心实体：Department、Employee。职责：工号生成、部门归属。
_Avoid_: 登录账号（User，规划中）

**盘点上下文（Stocktake）**：
核对系统记录与实际设备。核心实体：StocktakeSession、StocktakeRecord。产出盘盈盘亏差异。
_Avoid_: 日常状态变更（盘点只记录差异，不替代生命周期流转）

**系统管理上下文（System）**：
认证（当前单管理员）、系统日志、标签打印、数据导入导出。核心实体：Admin、SystemLog。
_Avoid_: 业务域日志混入安全审计

**报表统计上下文（Reporting）**：
多维统计投影（状态/分类/部门/员工/配件/流转趋势），无独立实体，只读聚合。
_Avoid_: 把统计口径散落到各业务上下文

**（规划中）审批上下文（Approval）**：
提交-审批-执行的通用工作流。实体：ApprovalFlow / ApprovalInstance / ApprovalRecord。通过后回调业务上下文执行动作。
_Avoid_: 把审批规则写死在资产生命周期里

**（规划中）通知上下文（Notification）**：
事件驱动的多通道通知，订阅生命周期与审批事件。
_Avoid_: 各业务上下文各自发通知

**（规划中）身份与访问上下文（IAM）**：
多账户、角色、权限、数据范围，横切所有业务上下文。
_Avoid_: 权限判定散落在单个业务动作里

## Business Rules

**R1 编号**：AssetNo = 分类 code 前缀 + 序号（如 DN-0001，按分类编号规则）；EmployeeNo = EMP + 4 位自增，创建员工时自动生成，无需手填。

**R2 设备状态机**：IDLE / IN_STOCK 可分配 → IN_USE；IN_USE 可归还 → IDLE、可调拨（更换使用人）、可送修 → IN_MAINTENANCE；维修完成回到可用；任意状态可报废 → SCRAPPED。报废保留记录、配件不回库。

**R3 设备名语义**：设备名跟随归属——在用 = 使用人姓名；闲置/库存 = 「闲置」；分配/归还/调拨时同步改名。

**R4 唯一设备约束**：分类标记 unique 时，同一员工在该分类下只能持有一台（分配/调拨时校验，冲突拒绝）。

**R5 库存规则**：采购入库增加；按模板组装出库扣减；升级扣新配件、旧配件回库；报废不回库。

**R6 删除边界**：设备「删除」= 硬删除（级联配件与履历，不可逆）；「报废」= 状态变更（保留全部记录）。有关联设备的员工不可删除。

**R7 盘点规则**：一次盘点任务内每台设备唯一；记录期望状态与实际结果（正常 / 盘亏 / 盘盈）。

**R8 状态展示**：设备状态一律中文（IN_STOCK → 库存，其余枚举有固定中文），界面不得出现英文枚举。

## Algorithm Boundaries

- **编号生成算法** → 资产上下文（AssetNo）/ 组织上下文（EmployeeNo）
- **资产生命周期状态机与履历** → 资产管理上下文
- **库存增减（采购入库 / 组装出库 / 升级扣回）** → 配件库存上下文
- **设备配置聚合（容量、硬盘、配置摘要）** → 资产管理上下文（展示投影）
- **统计与趋势聚合（状态 / 分类 / 部门×分类 / 生命周期）** → 报表统计上下文（只读）
- **（规划）审批引擎（状态推进、节点解析）** → 审批上下文；通过后跨上下文回调业务动作
- **（规划）通知投递** → 通知上下文，订阅生命周期 / 审批事件
- **（规划）权限判定与数据范围** → IAM 上下文，横切所有业务上下文
