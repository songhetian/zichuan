我将分两步为您优化仪表盘页面：

### 第一步：扩展数据接口 (src/database/db_manager.py)
为了支持更丰富的展示项目，我将在数据库管理类中添加 `get_dashboard_extended_stats` 方法，提供以下新数据：
1.  **本月新增入库数**：基于资产的 `purchase_date` 统计。
2.  **本月领用出库数**：基于 `lifecycle_logs` 的“领用”记录统计。
3.  **资产分类分布**：统计各分类下的资产数量，用于绘制分布条。

### 第二步：重构仪表盘 UI (src/ui/pages/dashboard_page.py)
我将全面升级仪表盘的视觉设计和布局：

1.  **顶部：关键指标卡片组 (Key Metrics)**
    *   **设计升级**：使用“卡片式+阴影”设计，增加 Emoji/图标装饰，采用更精致的排版（大号数字+描述文字）。
    *   **展示内容**：
        *   📦 **资产总数** (Total Assets)
        *   🚀 **在用资产** (In Use) - 增加“使用率”百分比展示
        *   📥 **本月入库** (New This Month) - 新增
        *   📤 **本月领用** (Assigned) - 新增

2.  **中部：左右分栏布局**
    *   **左侧 (60%)：资产分布概览**
        *   设计一个“分类统计面板”，使用美化的进度条 (Progress Bar) 直观展示各分类（如电脑、服务器、外设）的资产数量占比。
    *   **右侧 (40%)：最近动态时间轴**
        *   优化现有的日志列表，将其改造成“时间轴 (Timeline)”风格。
        *   每条动态增加图标背景（如领用用蓝色圆点，归还用绿色圆点），增强视觉引导。

3.  **视觉风格微调**
    *   引入更柔和的背景色和卡片圆角。
    *   优化字体层级，让核心数据更突出。
    *   增加鼠标悬停 (Hover) 的微交互效果。

这个方案将在保持现有系统风格一致的前提下，显著提升仪表盘的“精致感”和信息密度。