# 2026-08-06 工作日志

## 修复：测试套件并未全绿（用户问“现在完美了吧”时发现有 2 处挂）
- 之前会话总结称 370/370 全过，但实际有两个 untracked 测试文件处于漂移状态（测试存在但实现没跟上）：
  1. `tests/searchable-select-width.test.tsx` 第三个用例要求下拉项带 `truncate` 类，但 `src/components/ui/searchable-select.tsx` 的 `CommandPrimitive.Item` 只渲染纯文本。修复：把 `{option.label}` 包进 `<span className="truncate">`。
  2. `tests/asset-capacity.test.ts` 引用 `@/lib/asset-capacity` 的 `computeAssetCapacities`/`BOMComponent`，但该模块在 git 历史中从未存在——容量逻辑当时被内联进了 `src/actions/asset.actions.ts` 的 `extractCapacityGB` + 两个筛选循环。修复：新建 `src/lib/asset-capacity.ts`（已实现并通过测试的契约），并重构 `asset.actions.ts` 改为 `import { computeAssetCapacities }`，删除重复内联代码。

## 验证结果
- 前端 vitest：370/370 全过（修复前 1 文件失败 / 1 用例失败）
- Python unittest（tests/test_hardware_utils.py）：27/27 OK
- `tsc --noEmit` 类型检查通过

## 经验
- 跨会话宣称“全绿”不可轻信：untracked 测试文件容易漂移（测试写了但模块没建/组件没改）。验收时应真跑 `vitest run` 看红色，而非依赖会话总结数字。
