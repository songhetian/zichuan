# 2026-07-14

## 生成 domain.md（/setup-matt-pocock-skills）

- 读取了项目全部文档与源码（功能.txt、requirements.txt、config.py、db_manager.py、utils/*、ui/*、ui/pages/*、data/zichan.db 真实 schema）。
- 在仓库根目录生成 `domain.md`：技术栈、分层规范、DB 表结构、统一返回格式/接口契约、错误码、校验规则、业务约束、模块映射、需求对照、已知不一致清单（§10）。
- 按 matt-pocock skill 框架补齐脚手架：`CLAUDE.md`（## Agent skills 块）、`docs/agents/{domain,issue-tracker,triage-labels}.md`。
- 治理规则（最高约束）：后续诊断/重构/优化以 domain.md + 原始文档为唯一标准；代码与文档冲突以文档为准；不擅自改业务逻辑/表结构/接口出入参。
- 关键发现（写入 §10）：DAL 缺 4 个 UI 调用的方法（add_employee/update_employee/get_assets_by_status/auto_complete_stocktake）；init_db 与磁盘 schema 漂移（assets 缺 sn/price/image_path）；system_logs 从未被写入（log_action 无调用点）；lifecycle 仅 领用/归还；配置降级不回补库存。
- git remote 为 GitHub（songhetian/zichuan），issue tracker 默认按 GitHub 配置，PR 非 triage 面。
