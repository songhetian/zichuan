# 简单单用户认证

系统采用单用户简单登录，只有一个管理员账号，无复杂权限体系。

**原因**：
- 用户明确表示系统定位是给一个人用的，不需要复杂的权限判断
- 单用户系统做多用户权限是过度设计
- 保留一道基本的安全门，防止误操作
- 实现成本极低

**Consequences**：
- 没有角色和权限系统，所有操作都允许
- 只有一个 admin 账号，密码可修改
- 操作日志中的操作员固定为 admin
- 以后如需多用户，需要增加用户和权限模块
